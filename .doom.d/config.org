#+TITLE: My Doom Emacs config
#+AUTHOR: Shrishjay Acharya
#+PROPERTY: header-args :tangle config.el
#+DESCRIPTION: Shrishjay's personal Doom Emacs config.
#+STARTUP: showeverything
* TABLE OF CONTENTS :toc:
- [[#about-this-config][ABOUT THIS CONFIG]]
- [[#font-theme-and-ui][FONT, THEME AND UI]]
- [[#completion][COMPLETION]]
- [[#org-mode][ORG MODE]]
- [[#treemacs][TREEMACS]]
- [[#file-search][FILE SEARCH]]
- [[#pdf][PDF]]
- [[#treesitter][TREESITTER]]
- [[#vterm][VTERM]]

* ABOUT THIS CONFIG
This is my personal configuration for my doom emacs setup, i tried to keep this configuration minimal for my workflow.
* FONT, THEME AND UI
For font I use JetbrainsMono Nerd Font and catppuccin mocha as my theme.
#+begin_src emacs-lisp
;; (setq doom-font (font-spec :family "JetBrainsMono Nerd Font" :size 15))
(custom-set-faces!
  '(default :background "#16181c")
  '(solaire-default-face :background "#16181c")) 
(setq display-line-numbers-type 'relative)
(setq fancy-splash-image (concat doom-private-dir "media/splash.png"))
(use-package dired)
(add-hook 'dired-mode-hook 'auto-revert-mode)
(setq-default header-line-format " ")
#+end_src

#+RESULTS:

* COMPLETION
For autocompletion i use corfu with cape and capf
#+begin_src emacs-lisp

;; Define the function first (always available)
(after! corfu
  (setq corfu-auto t
        corfu-auto-prefix 1
        corfu-auto-delay 0.05))
(defun mi/eglot-capf-with-yasnippet ()
 (setq-local completion-at-point-functions
             (list
	       (cape-capf-super
		#'eglot-completion-at-point
		#'yasnippet-capf))))
(with-eval-after-load 'eglot
 (add-hook 'eglot-managed-mode-hook #'mi/eglot-capf-with-yasnippet))
(map! :leader
      :desc "Enable Eglot in buffer"
      "e" #'eglot)
(add-hook 'python-ts-mode-hook 
          (lambda ()
            (run-with-timer 0.0 nil #'eglot-ensure)))
(add-hook 'inferior-python-mode-hook
          (lambda ()
            (corfu-mode -1)))

#+end_src

* ORG MODE
#+begin_src emacs-lisp
(setq org-directory "~/org/")
(setq org-agenda-files '("~/org/agenda/"))
(setq org-clock-sound "~/.doom.d/media/beep.wav")
(after! org
  (custom-set-faces!
    '(org-table :foreground "cdd6f4" :weight normal))) ; use a brighter color
#+end_src

* TREEMACS
#+begin_src emacs-lisp
(map! :leader
      :desc "Toggle Treemacs"
      "ft" #'treemacs)
(setq treemacs-show-hidden-files nil)
#+end_src
* FILE SEARCH
#+begin_src emacs-lisp
(defun my/consult-fd-from-home ()
  "Find file from home directory using consult-fd."
  (interactive)
  (let ((default-directory (expand-file-name "~")))
    (consult-fd)))
(map! :leader
      :desc "Find file from ~"
      "f z" #'my/consult-fd-from-home)
(map! :leader
      :desc "Find file from directory"
      :"f a" #'consult-fd)
#+end_src
* PDF
#+begin_src emacs-lisp
(after! pdf-tools
  (map! :map pdf-view-mode-map
        :n "C-=" #'pdf-view-enlarge
        :n "C--" #'pdf-view-shrink
        :n "C-0" #'pdf-view-scale-reset
        :n "n"   #'pdf-view-next-page
        :n "p"   #'pdf-view-previous-page))
#+end_src
* TREESITTER
#+begin_src emacs-lisp
(use-package! treesit
  :config
  ;; Automatically use tree-sitter modes when available
  (setq treesit-font-lock-level 4) ;; Maximum highlighting detail
  
  ;; Remap major modes to their tree-sitter variants
  (setq major-mode-remap-alist
        '((python-mode . python-ts-mode))))
#+end_src
* VTERM
#+begin_src emacs-lisp
(map! :leader
      :desc "Open vterm"
      "v t" #'vterm)
#+end_src
